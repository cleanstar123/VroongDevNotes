# 가맹 협력사 주정산 & 선정산 개발 정리

> 작성일: 2026-04-15
> 프로젝트: vroong-backend  
> 브랜치: dev

---

## 1. 개요

가맹 협력사(`suppType='F'`)의 **주정산(weekly settlement)** 과 **선정산(pre-settlement)** 정산 내역을 조회·관리하는 기능을 개발하였다.

| 구분              | 설명                                       | 기준 테이블                                          |
| --------------- | ---------------------------------------- | ----------------------------------------------- |
| **주정산 (갑지 정산)** | 협력사 주 단위 정산. 엑셀 업로드로 데이터 등록 후 조회         | `vr_supp_acct_weekly`                           |
| **선정산 (을지 정산)** | 라이더 일별 선지급 정산. 배송일 기준 일별 집계              | `vr_rider_acct_weekly` (일별: `rider_acct_daily`) |
| **가맹 정산**       | 가맹 협력사(`suppType='F'`)만 필터링하여 별도 공식으로 계산 | `vr_supp_acct_weekly` + `vr_supp_acct_daily`    |

---

## 2. 전체 아키텍처

```
Client (Portal 프론트엔드)
    │
    ▼
[Controller Layer]
    FranchiseSettlementController     /api/v1/franchise-settlement
    PartnerSettlementController       /api/v1/partner-settlement
    RiderPreSettlementController      /api/v1/rider-pre-settlement
    │
    ▼
[UseCase Layer (Domain)]
    FranchiseSettlementUseCase        가맹 협력사 주정산 계산
    PartnerSettlementUseCase          일반 협력사 주정산 조회/엑셀
    RiderPreSettlementUseCase         라이더 선정산 조회/엑셀
    │
    ▼
[Repository Layer]
    PartnerSettlementRepository           (interface)
    PartnerSettlementRepositoryImpl       (QueryDSL / JPA)
    RiderPreSettlementRepository          (interface)
    RiderPreSettlementRepositoryImpl      (QueryDSL)
    SuppAcctWeeklyJpaRepository           (Spring Data JPA)
    VrSuppAcctDailyQueryDslRepository     (QueryDSL)
    VrSupplierInfoQueryDslRepository      (QueryDSL - 가맹 필터링)
    │
    ▼
[DB]
    Portal Schema   → vr_supp_acct_weekly, vr_supp_acct_weekly_his
                      vr_rider_acct_weekly, rider_acct_daily
    Vroong Schema   → vr_supp_acct_daily, vr_rider_acct_daily
                      vr_supplier_info (SUPP_TYPE='F' 판별)
```

---

## 3. DB 테이블 구조

### 3-1. `vr_supp_acct_weekly` — 협력사 주정산 (갑지)

가맹·일반 협력사 모두 여기에 저장. 엑셀 업로드로 데이터 등록됨.

| 컬럼 | 타입 | 설명 |
|------|------|------|
| SEQ_NO | PK | 일련번호 |
| ACCT_ST_DATE | String(8) | 정산 시작일 (YYYYMMDD) |
| ACCT_ED_DATE | String(8) | 정산 종료일 (YYYYMMDD) |
| SUPPLY_ID | String(50) | 협력사 ID |
| BIZ_REG_NO | String(20) | 사업자번호 |
| BIZ_NAME | String(100) | 사업자명(대표자명) |
| DELIVERY_VAL | BigDecimal | 배달료 |
| ADD_ACCT_VAL | BigDecimal | 후정산 |
| MANAGE_VAL | BigDecimal | 관리비 |
| MAINTNC_VAL | BigDecimal | 유지보수비 |
| MANAGE_FEE | BigDecimal | 관리수수료 |
| SUPPLY_AMT | BigDecimal | 공급가액 |
| TAX_VAT | BigDecimal | 부가세액 |
| SUPPLY_PRICE | BigDecimal | 공급대가 |
| ACHIEVEMENT_PAYBACK | BigDecimal | 달성페이백 |
| PROMOTION | BigDecimal | 프로모션 |
| PAYMENT_MGMT_FEE | BigDecimal | 지급관리비(VAT 포함) |
| SUPP_EMP_VAL | BigDecimal | 고용보험료 (사업주 부담) |
| RIDER_EMP_VAL | BigDecimal | 고용보험료 (라이더 부담) |
| SUPP_ACCIDT_VAL | BigDecimal | 산재보험료 (사업주 부담) |
| RIDER_ACCIDT_VAL | BigDecimal | 산재보험료 (라이더 부담) |
| TOT_INSURE_VAL | BigDecimal | 시간제보험료 합계 |
| INSURANCE_TOTAL | BigDecimal | 보험료 총합 |
| DEDUCT_VAL | BigDecimal | 일차감 |
| RET_EMP_VAL | BigDecimal | 소급 고용보험 |
| RET_ACCIDT_VAL | BigDecimal | 소급 산재보험 |
| TOT_RET_EMP_VAL | BigDecimal | 소급 합계 |
| RESID_TAX | BigDecimal | 지방세 |
| INCOM_TAX | BigDecimal | 소득세 |
| WITHHOLD_TAX | BigDecimal | 원천징수 합계 |
| ACCT_VAL | BigDecimal | 최종 정산금액 |
| PROC_CNT | BigDecimal | 처리건수 |

> `vr_supp_acct_weekly_his`: 엑셀 재업로드 또는 원장 적용 시 기존 데이터를 백업하는 히스토리 테이블

---

### 3-2. `vr_supp_acct_daily` — 가맹 협력사 일별 선정산

가맹 협력사의 일별 배달 집계. **가맹 정산 화면에서 선정산금액 합계 조회에 사용**.

| 컬럼 | 설명 |
|------|------|
| DELIVERY_DATE | 배달일자 (YYYYMMDD) |
| SUPPLY_ID | 협력사 ID |
| TOT_DELIVERY_CNT | 총 배달 건수 |
| TOT_DELIVERY_AMT | 총 배달금액 |
| PRE_SETTLEMENT_AMT | 선정산금액 |

---

### 3-3. `rider_acct_daily` / `vr_rider_acct_weekly` — 라이더 선정산

| 테이블 | 설명 |
|--------|------|
| `rider_acct_daily` | 라이더 일별 정산 집계 |
| `vr_rider_acct_weekly` | 라이더 주정산 집계 (을지) |

---

### 3-4. `vr_supplier_info` — 협력사 마스터

`SUPP_TYPE = 'F'` 인 협력사가 **가맹 협력사**. 이 값으로 필터링.

---

## 4. 가맹 협력사 주정산 (FranchiseSettlement)

### 4-1. API

```
GET /api/v1/franchise-settlement
    파라미터: startDate, endDate (정산시작일 범위), supplyId (선택)
    반환: FranchiseSettlementDto 목록

GET /api/v1/franchise-settlement/latest-date
    반환: latestStartDate, latestEndDate

GET /api/v1/franchise-settlement/suppliers
    반환: 가맹 협력사 목록 [{id, name}]
```

### 4-2. 처리 흐름

```
1. 건당 공제 금액 조회
   └─ vr_comm_code WHERE GRP_CODE='FRANC' AND COM_CODE='FEE_P'
      기본값: 30원

2. 협력사 주정산 데이터 조회
   └─ vr_supp_acct_weekly WHERE ACCT_ST_DATE BETWEEN startDate AND endDate
      (supplyId 있으면 추가 필터)

3. 가맹 협력사만 필터링
   └─ vr_supplier_info WHERE SUPP_ID IN (조회된 협력사 목록) AND SUPP_TYPE = 'F'

4. 협력사명 일괄 조회 (N+1 방지)
   └─ vr_supplier_info WHERE SUPP_ID IN (가맹 협력사 ID 목록)

5. 선정산금액 일괄 조회
   └─ vr_supp_acct_daily WHERE SUPPLY_ID IN (...) AND DELIVERY_DATE BETWEEN fromDate AND toDate
      → SUM(PRE_SETTLEMENT_AMT) GROUP BY SUPPLY_ID

6. 가맹 정산 계산 (협력사별 루프)
   └─ 아래 계산식 적용

7. FranchiseSettlementDto 빌드 후 반환
```

### 4-3. 가맹 정산 계산식

```
[부릉수수료]
  부릉수수료 = 처리건수(PROC_CNT) × 건당공제금액(기본 30원)

[공급가액]
  공급가액 = 배달료(DELIVERY_VAL) + 후정산(ADD_ACCT_VAL) + 관리비(MAINTNC_VAL) - 부릉수수료

[부가세액]
  부가세액 = 공급가액 × 10%  (소수점 이하 내림)

[공급대가]
  공급대가 = 공급가액 + 부가세액

[보험료합계]
  보험료합계 = 고용보험(사업주) + 고용보험(라이더) + 산재보험(사업주) + 산재보험(라이더) + 시간제보험료

[지급금액]
  지급금액 = 공급대가 - 보험료합계
```

### 4-4. 관련 클래스 (파일 경로)

| 역할 | 파일 |
|------|------|
| Controller | `api/.../controller/portal/settlement/FranchiseSettlementController.java` |
| UseCase | `api/.../domain/portal/settlement/usecase/FranchiseSettlementUseCase.java` |
| Response DTO | `api/.../response/portal/settlement/FranchiseSettlementDto.java` |
| Entity | `data/.../entity/portal/settlement/SuppAcctWeeklyEntity.java` |
| JPA Repository | `api/.../repository/portal/settlement/SuppAcctWeeklyJpaRepository.java` |
| 가맹 필터 | `api/.../repository/vroong/VrSupplierInfoQueryDslRepository.java` |
| 선정산 조회 | `api/.../repository/vroong/VrSuppAcctDailyQueryDslRepository.java` |

---

## 5. 협력사 주정산 (PartnerSettlement)

가맹을 포함한 **전체 협력사**의 주정산 내역 관리. 엑셀 업로드로 데이터를 등록하고, 원장(`vr_account_mst`) 적용 기능 포함.

### 5-1. API

```
GET  /api/v1/partner-settlement
     파라미터: startDate, endDate (정산시작일 범위) 또는 acctEdDate (하위호환)
              supplyId (선택)

GET  /api/v1/partner-settlement/latest-date
     반환: latestStartDate, latestEndDate

POST /api/v1/partner-settlement/upload   (multipart/form-data)
     파라미터: file (Excel), userId
     동작: 엑셀 파싱 → 기존 데이터 히스토리 백업 → 삭제 후 재저장

GET  /api/v1/partner-settlement/excel
     파라미터: acctStDate, acctEdDate, supplyId
     반환: .xlsx 파일 다운로드
```

### 5-2. 엑셀 업로드 흐름

```
1. 파일 유효성 검증 (확장자, 빈 파일 등)
2. 엑셀 파싱 → List<PartnerSettlement>
3. 파싱 데이터 유효성 검증
4. 동일 정산기간 기존 데이터 존재 여부 확인
   ├─ 존재하면: 히스토리 백업 (vr_supp_acct_weekly_his, reason='엑셀 재업로드') → 기존 삭제
   └─ 없으면: 최초 업로드
5. 새 데이터 저장 (vr_supp_acct_weekly)
※ 원장(vr_account_mst) 적용은 '적용' 버튼 클릭 시 별도 호출 (applyToAccountMst)
```

### 5-3. 협력사 주정산 계산식

```
[지급관리비 (VAT 포함)]
  지급관리비 = (관리비 + 지점운영수수료 + 달성페이백 + 프로모션) × 1.1

[실 지급 금액]
  실 지급 금액 = 지급관리비(VAT 포함) + 일차감 - 보험료합계
```

### 5-4. PartnerSettlement DTO 주요 필드

```
협력사명     → vr_supplier_info.SUPP_NAME (항상 DB 조회)
사업자번호   → vr_supplier_info.SUPP_NUM (항상 DB 조회)
대표자명     → vr_supplier_info.COMP_CEO_NAME (항상 DB 조회)
```
> `vr_supp_acct_weekly` 에 저장된 `BIZ_REG_NO`, `BIZ_NAME` 대신
> 항상 `vr_supplier_info` 에서 최신값을 조회하도록 구현.

### 5-5. 히스토리 테이블 활용 (`vr_supp_acct_weekly_his`)

- 엑셀 재업로드 시 기존 데이터를 자동 백업
- 원장 적용 시도 전에도 백업 (`reason='주정산 적용'`)
- `HIS_SEQ_NO`, `ARCHIVED_AT`, `RECALC_REASON` 컬럼 추가

---

## 6. 라이더 선정산 (RiderPreSettlement)

라이더의 일별 선지급 정산 내역 조회 및 엑셀 다운로드.

### 6-1. API

```
GET /api/v1/rider-pre-settlement
    파라미터: acctStDate, acctEdDate (필수), riderName (선택), supplierId (선택)
    반환: RiderPreSettlementDto 목록

GET /api/v1/rider-pre-settlement/latest-date
    파라미터: supplierId (선택)
    반환: latestStartDate (= latestEndDate - 6일), latestEndDate

GET /api/v1/rider-pre-settlement/excel
    파라미터: acctStDate, acctEdDate, riderName, supplierId
    반환: {날짜범위}_라이더선정산내역.xlsx
```

### 6-2. 처리 흐름

```
1. 날짜 범위 유효성 검증 (시작일 > 종료일이면 예외)

2. DB 조회
   └─ rider_acct_daily WHERE DELIVERY_DATE BETWEEN acctStDate AND acctEdDate
      (supplierId 있으면 추가 필터)

3. 라이더 정보 보강 (enrichSettlementsWithRiderInfo)
   ├─ 협력사 ID 목록 추출 → supplierRepository.findBySuppIdIn() (배치)
   ├─ 라이더 ID 목록 추출 → riderRepository.findByRiderIdIn() (배치)
   ├─ CompletableFuture.allOf()로 병렬 조회
   └─ 라이더 타입 변환:
       "65_YEARS_AGE"    → "만65세이상"
       "BRANCH_MANAGER"  → "지점장"
       default           → "일반"

4. 라이더명 필터링 (riderName 파라미터 있을 경우)
   └─ 라이더명 부분 일치(contains) 필터

5. RiderPreSettlementDto 변환
   └─ 지급일 = 배송일자 + 1일
```

### 6-3. 선정산 계산 항목

라이더 선정산은 계산을 직접 하지 않고 DB에 저장된 값을 그대로 조회.
(배치 또는 외부 시스템에서 `rider_acct_daily` 에 집계된 값)

| 항목 | 컬럼 | 설명 |
|------|------|------|
| 처리건수 | DELIVERY_CNT | 배달 건수 |
| 배달료 | DELIVERY_VAL | 배달 기본료 |
| 중개사운영수수료 | BROKER_MANAGE_FEE | 중개 수수료 |
| 협력사운영수수료 | PARTNER_MANAGE_FEE | 협력사 수수료 |
| 총운영수수료 | TOTAL_MANAGE_FEE | 합계 |
| 과세배달료 | TAXABLE_DELIVERY_VAL | 세금 대상 배달료 |
| 월보수액 | MONTHLY_SALARY | 월 기준 보수 |
| 라이더_고용보험 | RIDER_EMP_VAL | 고용보험 (라이더) |
| 라이더_산재보험 | RIDER_ACCIDT_VAL | 산재보험 (라이더) |
| 시간제보험료 | TIME_INSUR_VAL | 시간제 보험 |
| 보험료총합 | INSUR_TOT_VAL | 보험 합계 |
| 소득세 | INCOM_TAX | 소득세 |
| 지방세 | LOCAL_TAX | 지방세 |
| 원천징수 | WITHHOLD_TAX | 원천징수 합계 |
| 일차감 | DEDUCT_VAL | 공제 항목 |
| 선지급배달료 | PRE_ACCT_VAL | 실 지급액 |

### 6-4. 엑셀 다운로드 구성 (23컬럼)

```
NO | 협력사명 | 라이더ID | 라이더명 | 라이더구분 | 배송일자 | 지급일
처리건수 | 배달료 | 중개사운영수수료 | 협력사운영수수료 | 총운영수수료
과세배달료 | 월보수액 | 라이더_고용 | 라이더_산재 | 시간제보험료 | 총합
소득세 | 지방세 | 원천징수 | 일차감 | 선지급배달료
```

---

## 7. 주요 구현 포인트

### 7-1. 가맹 협력사 필터링

`vr_supplier_info.SUPP_TYPE = 'F'` 로 가맹 협력사를 식별.

```java
// VrSupplierInfoQueryDslRepository.java
public Set<String> findFranchiseSupplyIds(List<String> supplyIds) {
    return queryFactory
        .select(vsi.suppId)
        .from(vsi)
        .where(vsi.suppId.in(supplyIds).and(vsi.suppType.eq("F")))
        .fetch();
}
```

### 7-2. N+1 문제 방지 (배치 조회)

협력사명, 라이더 정보를 개별 조회하지 않고 ID 목록으로 한 번에 조회.

```java
// 협력사 목록 한번에 조회
supplierRepository.findBySuppIdIn(supplierIds)

// 라이더 목록 한번에 조회
riderRepository.findByRiderIdIn(riderIds)
```

### 7-3. 비동기 병렬 조회 (CompletableFuture)

협력사명 조회와 라이더 조회를 동시에 실행.

```java
CompletableFuture<Map<String, String>> supplierMapFuture = loadSupplierNames(supplierIds);
CompletableFuture<Map<String, Rider>> riderMapFuture = loadRiderMap(riderIds);

CompletableFuture.allOf(supplierMapFuture, riderMapFuture)
    .thenApply(v -> {
        // 둘 다 완료된 후 처리
    });
```

### 7-4. 공통코드 기반 설정값 관리

건당 공제 금액을 하드코딩하지 않고 `vr_comm_code` 에서 동적으로 조회.

```java
// GRP_CODE='FRANC', COM_CODE='FEE_P' 에 저장된 VAL1 값 사용
// 조회 실패 시 기본값 30원 적용
private static final String FRANCHISE_GRP_CODE = "FRANC";
private static final String FEE_PER_COUNT_CODE = "FEE_P";
```

### 7-5. 히스토리 백업 전략

데이터 수정 전에 히스토리 테이블에 백업 → 감사 추적(Audit Trail) 가능.

```
엑셀 재업로드 시:
  vr_supp_acct_weekly → vr_supp_acct_weekly_his (reason='엑셀 재업로드')
  기존 삭제 → 신규 저장

원장 적용 시:
  vr_supp_acct_weekly → vr_supp_acct_weekly_his (reason='주정산 적용')
  → vr_account_mst 업데이트
```

---

## 8. 전체 데이터 흐름 요약

### 가맹 협력사 주정산 흐름

```
[엑셀 파일 (정산 데이터)]
        ↓ POST /api/v1/partner-settlement/upload
[PartnerSettlementController]
        ↓
[PartnerSettlementUseCase.uploadExcel()]
        ↓ 파싱 + 히스토리 백업
[vr_supp_acct_weekly 저장]
        ↓

[GET /api/v1/franchise-settlement]
        ↓
[FranchiseSettlementUseCase.getFranchiseSettlementList()]
        ├─ vr_supp_acct_weekly 조회 (기간 필터)
        ├─ vr_supplier_info WHERE SUPP_TYPE='F' 로 가맹 필터링
        ├─ vr_supplier_info 에서 협력사명 배치 조회
        ├─ vr_supp_acct_daily 에서 선정산금액 합계 조회
        └─ 가맹 정산 계산 (공급가액 → 부가세 → 공급대가 → 지급금액)
        ↓
[FranchiseSettlementDto 반환]
```

### 라이더 선정산 흐름

```
[외부 시스템 / 배치]
        ↓ rider_acct_daily 집계 저장
[rider_acct_daily]
        ↓

[GET /api/v1/rider-pre-settlement]
        ↓
[RiderPreSettlementController]
        ↓
[RiderPreSettlementUseCase.getSettlementList()]
        ├─ rider_acct_daily 조회 (기간 + 라이더/협력사 필터)
        ├─ supplierRepository.findBySuppIdIn() — 협력사명 배치 조회
        ├─ riderRepository.findByRiderIdIn() — 라이더 정보 배치 조회 (비동기 병렬)
        ├─ 라이더 타입 코드 → 한글 변환
        └─ 라이더명 필터링 (riderName 파라미터)
        ↓
[RiderPreSettlementDto 반환 / 엑셀 다운로드]
```

---

## 9. 핵심 상수 정리

| 상수 | 값 | 설명 |
|------|-----|------|
| `FRANCHISE_GRP_CODE` | `"FRANC"` | 공통코드 그룹 (가맹 수수료) |
| `FEE_PER_COUNT_CODE` | `"FEE_P"` | 건당 공제 금액 코드 |
| `VAT_TAX_RATE` | `0.1` | 부가세율 10% |
| `VAT_RATE` | `1.1` | VAT 포함 배율 |
| `MONTHLY_SALARY_RATE` | `0.802` | 월보수 비율 80.2% |
| `EMPLOYMENT_RATE` | `0.008` | 고용보험률 0.8% |
| `INDUSTRIAL_RATE` | `0.0088` | 산재보험률 0.88% |
| 기본 건당 공제 금액 | `30원` | DB 조회 실패 시 fallback |

---

## 10. 회고 및 주의사항

1. **가맹 협력사 필터링 위치**: `vr_supp_acct_weekly` 에는 모든 협력사 데이터가 저장되므로, 조회 후 `vr_supplier_info.SUPP_TYPE='F'` 로 인메모리 필터링한다. 데이터 볼륨이 커지면 쿼리 단에서 JOIN으로 처리하는 것도 고려.

2. **협력사명/사업자번호 조회**: `vr_supp_acct_weekly` 에 저장된 값 대신 항상 `vr_supplier_info` 에서 최신 정보를 조회함. 마스터 데이터 변경 시 일관성 유지.

3. **선정산금액**: `vr_supp_acct_daily.PRE_SETTLEMENT_AMT` 를 기간별 SUM하여 가맹 정산 화면에 표시. 별도 계산 없이 이미 집계된 값을 참조.

4. **비동기 처리**: `CompletableFuture` 를 전 레이어에 걸쳐 사용. 컨트롤러 반환 타입도 `CompletableFuture<ResponseEntity<...>>` 로 통일.

5. **히스토리 보존**: 재업로드/원장 적용 전에 반드시 히스토리 백업 → 데이터 추적 가능.
