# 2026-02-12 개발 일지

#daily #vroong #baemin #dual-session #logout #alb #sms-webhook #auto-reauth #audit-log #cloudwatch

---

## 📊 오늘 작업 요약

| 작업                                | 상태  | 분류           |
| --------------------------------- | :-: | ------------ |
| relay-server 코드 복원 (자동 로그인 성공 시점) |  ✅  | Rollback     |
| 서버 배포 시 로그아웃 기능 추가                |  ✅  | Feature      |
| 듀얼 세션 매니저 통합                      |  ✅  | Feature      |
| SMS 웹훅 URL 문제 해결                  |  ✅  | Bug Fix      |
| ALB 8090 포트 리스너 추가                |  ✅  | Infra        |
| 듀얼 세션 완전 동작 확인                    |  ✅  | Verification |
| **AOP 기반 감사 로그 시스템 구현**           |  ✅  | Feature      |
| **CloudWatch Logs 연동 확인**         |  ✅  | Infra        |

---

## 1. relay-server 코드 복원

> [!info] 배경
> - 어제 수정한 코드들이 여러 문제를 일으킴
> - 자동 재인증 성공했던 시점으로 되돌리기 필요

> [!success] 해결
> - git 커밋 `f89d6c7 자동 로그인 성공 시점`으로 복원
> - 명령어: `git checkout f89d6c7 -- relay-server/`
> - relay-server 폴더만 선택적으로 복원

> [!tip] 배운 점
> - git checkout으로 특정 폴더만 과거 커밋으로 복원 가능
> - 복원 후 스테이징 해제: `git restore --staged relay-server/`

---

## 2. 서버 배포 시 로그아웃 기능 추가

> [!info] 배경
> - ECS 서비스 재배포 시 기존 세션이 살아있으면 충돌 가능
> - 새 배포 시 기존 세션을 정리하고 새로 로그인해야 함

> [!warning] 문제
> - 배포 후 세션 상태가 불일치할 수 있음
> - 이전 세션 쿠키가 무효화되어 API 호출 실패

> [!success] 해결
> - `logout()` 함수 추가 (`services/baemin/auth.js`)
> - 배민 로그아웃 API 호출: `POST https://api-deliverycenter.baemin.com/logout`
> - 서버 시작 시 기존 세션 로그아웃 후 재인증 시작

### 수정된 파일
```
relay-server/services/baemin/auth.js      - logout() 함수 추가
relay-server/services/baemin/constants.js - BAEMIN_API.LOGOUT URL 추가
relay-server/services/baemin/index.js     - logout export 추가
relay-server/server.js                    - 서버 시작 시 로그아웃 로직
```

### 로그아웃 함수
```javascript
async function logout(session) {
    const page = browserManager.getPage(sessionId);

    // 페이지 컨텍스트에서 로그아웃 API 호출
    const logoutResult = await page.evaluate(async (logoutUrl, centerId) => {
        const response = await fetch(logoutUrl, {
            method: 'POST',
            credentials: 'include',
            headers: {
                'Content-Type': 'application/json',
                'center-id': centerId || '',
            }
        });
        return { success: response.ok };
    }, BAEMIN_API.LOGOUT, centerId);

    // 세션 삭제
    await browserManager.destroySession(sessionId);
}
```

---

## 3. 듀얼 세션 매니저 통합

> [!info] 배경
> - 하나의 세션이 만료되면 배치 작업이 중단됨
> - 무중단 서비스를 위해 백업 세션 필요

> [!success] 해결
> - 기존 `dual-session-manager.js` 활용
> - `server.js`에 통합 및 API 엔드포인트 추가

### 듀얼 세션 구조
```
Primary 세션 (활성)  ← 모든 API 요청 처리
    ↓ 만료 감지
Backup 세션 (대기)  → Primary로 승격
    ↓
백그라운드에서 새 Backup 생성 (30초 후)
    ↓
무중단 서비스 유지!
```

### 추가된 API 엔드포인트
| 엔드포인트 | 설명 |
|-----------|------|
| `GET /relay/dual-session/status` | Primary/Backup 세션 상태 |
| `GET /relay/dual-session/active` | 현재 활성 세션 ID |
| `POST /relay/dual-session/start` | 듀얼 세션 수동 시작 |
| `POST /relay/dual-session/failover` | 수동 페일오버 (테스트용) |
| `POST /relay/dual-session/stop` | 듀얼 세션 정지 |

---

## 4. SMS 웹훅 URL 문제 해결

> [!info] 배경
> - MacroDroid 앱에서 SMS 웹훅 전송 시 200 응답을 받지만
> - relay-server에서 `[SMS 웹훅]` 로그가 찍히지 않음

> [!warning] 문제
> - 매크로 앱 URL: `http://vroong-alb:8080/api/v1/baemin/auth/sms-webhook`
> - relay-server 엔드포인트: `/webhook/sms`
> - **완전히 다른 경로로 요청 중!**
> - 8080 포트는 Spring Boot 백엔드로 연결됨

> [!success] 해결
> - 매크로 앱 URL 변경: `http://vroong-alb:8090/webhook/sms`
> - ALB에 8090 포트 리스너 추가 (아래 참조)

---

## 5. ALB 8090 포트 리스너 추가

> [!info] 배경
> - relay-server는 8090 포트에서 실행
> - 기존 vroong-alb는 8080(dev), 443(prod)만 리스닝

> [!warning] 문제
> 1. ALB에 8090 리스너 없음 → 504 Gateway Timeout
> 2. ALB 보안 그룹에 8090 인바운드 없음
> 3. ECS 보안 그룹에 ALB→8090 허용 없음
> 4. ALB 가용 영역(2a, 2b)과 ECS 가용 영역(2a~2d) 불일치 → 503 Service Unavailable

> [!success] 해결

### Step 1: 타겟 그룹 생성
- 이름: `vroong-relay-tg-dev`
- 타입: IP addresses
- 포트: 8090
- Health check: `/health`

### Step 2: ALB 리스너 추가
- Protocol: HTTP
- Port: 8090
- Target group: `vroong-relay-tg-dev`

### Step 3: ECS 서비스에 타겟 그룹 연결
- Load balancer: `vroong-alb`
- Listener: `8090:HTTP`
- Container: `relay:8090`

### Step 4: 보안 그룹 설정
```
# ALB 보안 그룹 (sg-023cedb908b4ac876)
Inbound: 8090 from 0.0.0.0/0

# ECS 태스크 보안 그룹 (sg-0a402d0b95bd18188)
Inbound: 8090 from sg-023cedb908b4ac876 (ALB)
```

### Step 5: ALB 가용 영역 추가
```
기존: ap-northeast-2a, ap-northeast-2b
추가: ap-northeast-2c (subnet-01ee3b942b61ba20c)
      ap-northeast-2d (subnet-09ecf54372be7116e)
```

> [!tip] 배운 점
> - ALB와 ECS 태스크의 가용 영역이 일치해야 함
> - 불일치 시 "Target is in an Availability Zone that is not enabled for the load balancer" 에러

---

## 6. 듀얼 세션 동작 확인

> [!success] 최종 결과

### 로그 확인
```
[10:16:44] [DualSession] 듀얼 세션 시작
[10:17:31] [SMS 웹훅] 내용: 024561
[10:17:38] [DualSession] Primary 세션 생성 완료: 97714636...
[10:18:08] [DualSession] Backup 세션 생성 중...
[10:19:01] [SMS 웹훅] 내용: 572701
[10:19:08] [DualSession] Backup 세션 생성 완료: b0400b1b...
[10:19:08] [DualSession] 듀얼 세션 시작 성공!
[10:19:08] [DualSession] Primary: 97714636
[10:19:08] [DualSession] Backup: b0400b1b
```

### 확인된 기능
- ✅ SMS 웹훅: MacroDroid → ALB:8090 → relay-server 정상 연결
- ✅ 자동 로그인: ID/PW 입력 → 2FA 자동 처리
- ✅ 듀얼 세션: Primary + Backup 두 세션 생성
- ✅ 헬스체크: 60초 간격으로 세션 상태 모니터링
- ✅ 협력사 조회: 58개 협력사 정상 로드

---

## 📋 TODO

- [x] relay-server 복원 (자동 로그인 성공 시점)
- [x] 로그아웃 기능 추가
- [x] 듀얼 세션 통합
- [x] SMS 웹훅 문제 해결
- [x] ALB 설정 완료
- [x] 듀얼 세션 동작 확인

---

## 📝 메모

### 매크로 앱 설정 (변경됨)
```
URL: http://vroong-alb-1049542169.ap-northeast-2.elb.amazonaws.com:8090/webhook/sms
Method: POST
Body: {"message": "{lv=auth_code}"}
Headers:
  Content-Type: application/json
```

### ECS 환경변수 (필수)
```
USE_PUPPETEER=true
BAEMIN_USERNAME=xxx
BAEMIN_PASSWORD=xxx
BAEMIN_PHONE=010xxxxxxxx
AWS_REDIS_URL=redis://your-redis:6379
```

### 배포 명령어
```bash
# 1. ECR 로그인
aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 151567229571.dkr.ecr.ap-northeast-2.amazonaws.com

# 2. Docker 빌드
docker build -f Dockerfile.puppeteer -t vroong/relay:dev .

# 3. 태그
docker tag vroong/relay:dev 151567229571.dkr.ecr.ap-northeast-2.amazonaws.com/vroong/relay:dev

# 4. 푸시
docker push 151567229571.dkr.ecr.ap-northeast-2.amazonaws.com/vroong/relay:dev

# 5. ECS 재배포
aws ecs update-service --cluster vroong-cluster --service vroong-relay-service-dev --force-new-deployment --region ap-northeast-2
```

### AWS 리소스 정리
```
ALB: vroong-alb
  - Listener 8090 → vroong-relay-tg-dev

Target Group: vroong-relay-tg-dev
  - Port: 8090
  - Health: /health

ECS Service: vroong-relay-service-dev
  - Task Definition: vroong-relay-task-dev
  - Container Port: 8090

Security Groups:
  - ALB: sg-023cedb908b4ac876 (inbound 8090)
  - ECS: sg-0a402d0b95bd18188 (inbound 8090 from ALB)
```

### 수정된 파일 (Relay Server)
```
relay-server/server.js                    - 듀얼 세션 통합, API 엔드포인트 추가
relay-server/services/auto-reauth.js      - 복원
relay-server/services/baemin/auth.js      - logout() 함수 추가, 복원
relay-server/services/baemin/constants.js - BAEMIN_API.LOGOUT 추가
relay-server/services/baemin/index.js     - logout export
relay-server/services/baemin/center.js    - 복원
relay-server/services/browser/core.js     - 복원
```

---

## 7. AOP 기반 감사 로그 시스템 구현

> [!info] 배경
> - 라이더 출금 시 `vr_withdraw_request_his`, `vr_withdrl_list`에는 기록이 있는데 `vr_account_mst`에 누락되는 케이스 발생
> - 중요한 비즈니스 로직(출금, 입금, 정산 등)에 대한 추적 로그 필요
> - 향후 문제 발생 시 원인 파악을 위한 감사 로그 시스템 구축

> [!success] 해결
> - Spring AOP 기반 `@AuditLog` 어노테이션 방식 구현
> - CloudWatch Logs로 자동 전송 (Fargate 환경)
> - 민감정보 자동 마스킹 (계좌번호, 비밀번호 등)

### 생성된 파일 구조
```
api/src/main/java/com/vroong/api/infrastructure/logging/
├── annotation/
│   └── AuditLog.java              # @AuditLog 어노테이션
├── aspect/
│   └── AuditLogAspect.java        # AOP Aspect (메서드 가로채기)
├── model/
│   ├── AuditLogCategory.java      # 로그 카테고리 enum
│   └── AuditLogEntry.java         # 로그 엔트리 모델
├── service/
│   └── AuditLogService.java       # 로그 기록 서비스
└── util/
    └── SensitiveDataMasker.java   # 민감정보 마스킹 유틸

api/src/main/resources/
└── logback-spring.xml             # Logback 설정 (프로파일별)
```

### 지원 카테고리
| 분류 | 카테고리 | 설명 |
|------|----------|------|
| 금융 | WITHDRAWAL, DEPOSIT, TRANSFER | 출금, 입금, 송금 |
| 정산 | PRE_SETTLEMENT, WEEKLY_SETTLEMENT | 선정산, 주정산 |
| 잔액 | BALANCE, HEAD_OFFICE_DEPOSIT | 잔액조회, 본사예치금 |
| 인증 | SIGNUP, LOGIN, LOGOUT, TOKEN_REFRESH | 회원가입, 로그인 등 |
| 외부API | HECTO_API, BAEMIN_API | 헥토, 배민 API |
| 배치 | BAEMIN_DELIVERY_BATCH, BAEMIN_RIDER_BATCH, BAEMIN_SETTLEMENT_BATCH | 배민 배치 |
| 릴레이 | RELAY_SESSION, RELAY_AUTO_REAUTH, RELAY_DUAL_SESSION | 릴레이 서버 |
| 공지사항 | NOTICE | 공지사항 CRUD |
| 시스템 | SYSTEM | 일반 시스템 |

### 사용 예시
```java
@AuditLog(
    category = AuditLogCategory.NOTICE,
    action = "CREATE",
    description = "공지사항 등록")
@Transactional
public CompletableFuture<Long> createNotice(CreateNoticeCommand command) {
    // 비즈니스 로직
}
```

### 로그 출력 예시
```json
[AUDIT] {
  "timestamp": "2026-02-12T16:41:00.356",
  "traceId": "22ef34af",
  "category": "NOTICE",
  "action": "CREATE",
  "description": "공지사항 등록",
  "className": "NoticeUseCase",
  "methodName": "createNotice",
  "status": "SUCCESS",
  "durationMs": 92,
  "request": {
    "command": {
      "title": "로그테스트입니다.",
      "content": "로그테스트입니다.",
      "targetType": "A",
      "targetScope": "ALL"
    }
  },
  "response": 17
}
```

### 환경별 로그 저장 위치
| 프로파일 | 로그 출력 | 저장 위치 |
|----------|----------|-----------|
| local | 파일 + 콘솔 | `logs/` 폴더 + 터미널 |
| dev | 콘솔만 | CloudWatch Logs |
| prod | 콘솔만 | CloudWatch Logs |

---

## 8. CloudWatch Logs 연동 확인

> [!info] 확인 내용
> - ECS 태스크 정의에 이미 CloudWatch 설정 완료됨
> - 추가 환경 세팅 불필요

### 기존 태스크 정의 (task-definition-dev.json)
```json
"logConfiguration": {
  "logDriver": "awslogs",
  "options": {
    "awslogs-group": "/ecs/vroong-api-dev",
    "awslogs-region": "ap-northeast-2",
    "awslogs-stream-prefix": "ecs",
    "awslogs-create-group": "true"
  }
}
```

### CloudWatch에서 감사 로그 검색
```sql
fields @timestamp, @message
| filter @message like /\[AUDIT\]/
| sort @timestamp desc
| limit 100
```

### 비용
- 로그 수집: $0.76/GB
- 로그 저장: $0.03/GB/월
- 예상 월 비용: **$1~5** (감사 로그 전체 적용 기준)

---

## 📋 TODO (감사 로그)

- [x] AOP 기반 감사 로그 시스템 구현
- [x] 공지사항 UseCase에 @AuditLog 적용 (테스트)
- [x] 로컬 테스트 완료
- [x] CloudWatch 연동 확인
- [ ] 개발 서버 배포 후 CloudWatch 로그 확인
- [ ] 출금/입금 UseCase에 @AuditLog 적용
- [ ] 정산 UseCase에 @AuditLog 적용
- [ ] 인증 관련 UseCase에 @AuditLog 적용

### 수정된 파일 (감사 로그)
```
api/src/main/java/com/vroong/api/infrastructure/logging/annotation/AuditLog.java
api/src/main/java/com/vroong/api/infrastructure/logging/aspect/AuditLogAspect.java
api/src/main/java/com/vroong/api/infrastructure/logging/model/AuditLogCategory.java
api/src/main/java/com/vroong/api/infrastructure/logging/model/AuditLogEntry.java
api/src/main/java/com/vroong/api/infrastructure/logging/service/AuditLogService.java
api/src/main/java/com/vroong/api/infrastructure/logging/util/SensitiveDataMasker.java
api/src/main/resources/logback-spring.xml
api/src/main/java/com/vroong/api/domain/portal/notice/usecase/NoticeUseCase.java
```
