# 2026-02-27 개발 일지

#daily #vroong

---

## 오늘 작업 요약

| 작업 | 상태 | 분류 |
|------|:----:|------|
| VROONG Swagger Tool 데스크탑 앱 개발 | 완료 | 기능개발 |
| 로그인 API 엔드포인트 및 Secret 수정 | 완료 | 버그수정 |
| 엑셀 내보내기 기능 구현 및 개선 | 완료 | 기능개발 |
| Form 파라미터 value/onChange 전달 수정 | 완료 | 버그수정 |
| 날짜 형식(dateFormat) 파라미터 지원 | 완료 | 기능개발 |

---

## 1. VROONG Swagger Tool 데스크탑 앱 개발

> [!info] 배경
> - 배치 프로세스 실패 또는 특정 기능 부재 시 수동으로 API를 호출해야 하는 상황 발생
> - Swagger UI 접근이 어렵거나 복잡한 파라미터 입력이 필요한 경우 불편함
> - 관리자가 쉽게 사용할 수 있는 GUI 도구 필요

> [!success] 구현 내용
> - Electron + React + TypeScript 기반 Windows 데스크탑 앱
> - Ant Design UI 라이브러리 사용
> - 10개 카테고리, 33개 API 지원
> - Windows 설치 파일(.exe) 생성

**기술 스택:**
- Electron 28.0.0
- React 18.2.0
- TypeScript 5.3.3
- Vite 5.0.10
- Ant Design 5.12.0
- Axios 1.6.2
- xlsx (엑셀 변환)
- file-saver (파일 다운로드)

**지원 API 카테고리:**
1. 협력사 관리 (5개 API)
2. 배달 관리 (8개 API)
3. 라이더 관리 (7개 API)
4. 협력사 목록 (1개 API)
5. 마이그레이션 관리 (3개 API)
6. 재마감 처리 (4개 API)
7. 정산서 업로드 (1개 API)
8. 사용자 관리 (1개 API)
9. 점검 관리 (2개 API)
10. 탈퇴요청 관리 (1개 API)

**생성된 파일:**
- `release/VROONG Swagger Tool Setup 1.0.0.exe` (약 88MB)

---

## 2. 로그인 API 엔드포인트 및 Secret 수정

> [!warning] 문제 1
> - `/api/v1/auth/login` 호출 시 401 Unauthorized
> - 해당 엔드포인트는 배민 인증용 (`BaeminAuthController`)

> [!success] 해결 1
> - 포털 로그인 API로 변경: `/api/v1/partner-portal/auth/login`
> - `PartnerPortalAuthController`의 `loginByPassword` 메서드 사용
> - vr_user_info 테이블 기반 인증

> [!warning] 문제 2
> - API 엔드포인트 수정 후에도 401 Unauthorized 발생
> - X-API-Secret 헤더 값이 잘못됨

> [!success] 해결 2
> - application-dev.yml 확인하여 올바른 Secret 적용
> - `local-development-secret-key` → `local-dev-secret-key-2024`

```typescript
// LoginPage.tsx
const response = await axios.post(
    `${baseUrl}/api/v1/partner-portal/auth/login`,
    { userId: values.userId, password: values.password },
    {
        headers: {
            'Content-Type': 'application/json',
            'X-API-Secret': 'local-dev-secret-key-2024',
        },
    }
);

// 관리자 권한 확인
if (!adminYn && userType !== 'HEADQUARTERS') {
    message.error('관리자(A) 권한이 필요합니다.');
    return;
}
```

**수정 파일:**
- `src/renderer/pages/LoginPage.tsx`
- `src/renderer/store/authStore.ts`

---

## 3. 엑셀 내보내기 기능 구현 및 개선

> [!info] 배경
> - API 호출 결과를 엑셀 파일로 다운로드하는 기능 필요
> - 복잡한 JSON 응답을 사람이 읽기 쉬운 형태로 변환 필요

### 3.1 초기 구현 문제

> [!warning] 문제
> - 배열 데이터(centers)가 JSON 문자열로 한 셀에 들어감
> - 예: `[{"id":"DP2504258339","name":"강원동해A부릉라이더스1",...},...]`

### 3.2 배열 데이터 행 펼침 개선

> [!success] 해결
> - 주요 배열 필드(centers, data, items 등) 자동 감지
> - 배열의 각 항목을 별도 행으로 표시

```typescript
// 목록 조회용 주요 배열 필드명
const listFieldNames = ['centers', 'data', 'items', 'list', 'riders',
    'deliveries', 'orders', 'results', 'records', 'users', 'partners', 'suppliers'];

// 응답 타입 판별
if (Array.isArray(responseData)) {
    exportData = responseData;
    isListResponse = true;
} else {
    for (const fieldName of listFieldNames) {
        if (Array.isArray(responseData[fieldName])) {
            exportData = responseData[fieldName];
            isListResponse = true;
            break;
        }
    }
    // 주요 배열 필드가 없으면 단일 상세 조회로 판단
    if (!isListResponse) {
        exportData = [responseData];
    }
}
```

### 3.3 단일 상세 조회 데이터 평탄화

> [!warning] 문제
> - 협력사 상세 정보 조회 시 `deliveryZone` 배열만 내보내짐
> - 전체 객체 정보(id, name, status, contractBusiness 등)가 누락됨

> [!success] 해결
> - 주요 배열 필드가 없으면 단일 상세 조회로 판단
> - 전체 객체를 평탄화하여 한 행으로 표시
> - 중첩 객체: `contractBusiness.businessName` → `contractBusiness_businessName`

### 3.4 중첩 배열 데이터 추출 (dongs)

> [!warning] 문제
> - `deliveryZone` 배열 내의 `dongs` 배열 데이터가 누락됨

> [!success] 해결
> - 중첩 배열의 각 필드별로 값 추출
> - 예: `deliveryZone_dongs_dongName` → "천곡동, 평릉동, 송정동, ..."

```typescript
// 중첩 배열 처리
if (Array.isArray(fieldValue) && typeof fieldValue[0] === 'object') {
    const nestedFields = Object.keys(fieldValue[0]);
    nestedFields.forEach(nestedKey => {
        const allNestedValues: string[] = [];
        value.forEach((parentItem: any) => {
            if (Array.isArray(parentItem[fieldKey])) {
                parentItem[fieldKey].forEach((nestedItem: any) => {
                    if (nestedItem[nestedKey] != null) {
                        allNestedValues.push(String(nestedItem[nestedKey]));
                    }
                });
            }
        });
        // 중복 제거
        const uniqueValues = [...new Set(allNestedValues)];
        result[`${fieldNewKey}_${nestedKey}`] = uniqueValues.join(', ');
    });
}
```

**최종 엑셀 출력 예시:**

| id | name | status_code | deliveryZone_zoneName | deliveryZone_dongs_dongName | contractBusiness_businessName |
|---|---|---|---|---|---|
| DP2504258339 | 강원동해A부릉라이더스1 | TERMINATED | 표준강원동해A | 천곡동, 평릉동, 송정동, ... | 주식회사 부릉라이더스 |

**수정 파일:**
- `src/renderer/components/ApiExecutor.tsx`

---

## 4. Form 파라미터 value/onChange 전달 수정

> [!warning] 문제
> - path 파라미터(centerId) 입력 후 실행 시 "centerId은(는) 필수입니다" 에러
> - 입력값이 Form에 전달되지 않음

> [!success] 해결
> - `ParameterInput` 컴포넌트에서 Form.Item이 주입하는 `value`와 `onChange` props를 하위 컴포넌트에 전달

```typescript
// 수정 전
const ParameterInput: React.FC<{ param: ApiParameter }> = ({ param }) => {
    return <Input placeholder={param.description} />;
};

// 수정 후
const ParameterInput: React.FC<{
    param: ApiParameter;
    value?: any;
    onChange?: (value: any) => void
}> = ({ param, value, onChange, ...rest }) => {
    return (
        <Input
            placeholder={param.description}
            value={value}
            onChange={onChange}
            {...rest}
        />
    );
};
```

**수정 파일:**
- `src/renderer/components/ApiExecutor.tsx`

---

## 5. 날짜 형식(dateFormat) 파라미터 지원

> [!warning] 문제
> - 협력사 배달비 정보 조회 API 호출 시 400 Bad Request
> - 에러: "날짜 형식은 YYYY-MM-DD여야 합니다"
> - 기존 코드는 모든 날짜를 `YYYYMMDD` 형식으로 변환

> [!success] 해결
> - `ApiParameter` 인터페이스에 `dateFormat` 필드 추가
> - API별로 날짜 형식 지정 가능 (`YYYYMMDD` 또는 `YYYY-MM-DD`)

```typescript
// apiEndpoints.ts - 인터페이스 확장
export interface ApiParameter {
    name: string;
    type: 'string' | 'number' | 'date' | 'boolean' | 'select';
    required: boolean;
    description: string;
    in: 'path' | 'query' | 'body';
    options?: { label: string; value: string }[];
    defaultValue?: string;
    dateFormat?: 'YYYYMMDD' | 'YYYY-MM-DD';  // 추가
}

// 협력사 배달비 정보 조회 API
{
    name: 'fromDate',
    type: 'date',
    required: true,
    description: '시작일 (YYYY-MM-DD)',
    in: 'query',
    dateFormat: 'YYYY-MM-DD',  // 형식 지정
}
```

```typescript
// ApiExecutor.tsx - 날짜 변환 시 dateFormat 적용
Object.keys(values).forEach((key) => {
    const value = values[key];
    if (value && dayjs.isDayjs(value)) {
        const param = endpoint.parameters.find(p => p.name === key);
        const dateFormat = param?.dateFormat || 'YYYYMMDD';
        processedValues[key] = value.format(dateFormat);
    } else {
        processedValues[key] = value;
    }
});
```

**수정 파일:**
- `src/renderer/config/apiEndpoints.ts`
- `src/renderer/components/ApiExecutor.tsx`

---

## 프로젝트 구조

```
vroong-swagger-tool/
├── src/
│   ├── main/                    # Electron 메인 프로세스
│   │   └── index.ts
│   └── renderer/                # React 렌더러
│       ├── api/
│       │   └── apiClient.ts     # API 호출 클라이언트
│       ├── components/
│       │   └── ApiExecutor.tsx  # API 실행 및 결과 표시
│       ├── config/
│       │   └── apiEndpoints.ts  # API 엔드포인트 정의
│       ├── pages/
│       │   ├── LoginPage.tsx    # 로그인 페이지
│       │   └── DashboardPage.tsx
│       ├── store/
│       │   └── authStore.ts     # 인증 상태 관리
│       └── styles/
├── package.json
├── vite.config.ts
├── tsconfig.json
└── electron-builder.yml
```

---

## TODO

- [ ] 추가 API 엔드포인트 필요 시 apiEndpoints.ts에 추가
- [ ] 배치 실행 API 추가 검토
- [ ] 앱 버전 업데이트 기능 검토

---

## 메모

- Electron + React 앱 빌드: `npm run package:win`
- 설치 파일 위치: `release/VROONG Swagger Tool Setup 1.0.0.exe`
- 로그인은 vr_user_info 테이블의 관리자(adminYn=true 또는 userType=HEADQUARTERS) 계정만 가능
- 개발 서버 API Secret: `local-dev-secret-key-2024`
- 날짜 형식은 API별로 다를 수 있음 (YYYYMMDD vs YYYY-MM-DD)
