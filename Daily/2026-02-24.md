# 2026-02-24 개발 일지

#daily #vroong #refactoring #clean-architecture

---

## 📊 오늘 작업 요약

| 작업 | 상태 | 분류 |
|------|:----:|------|
| 프로젝트 폴더 구조 리팩토링 | ✅ 완료 | 아키텍처 |
| 크롤링/배치/비즈니스 코드 분리 | ✅ 완료 | 리팩토링 |
| Clean Architecture 적용 | ✅ 완료 | 아키텍처 |

---

## 1. 프로젝트 폴더 구조 대규모 리팩토링

> [!info] 배경
> - 기존 프로젝트 폴더 구조가 혼재되어 어떤 파일이 어떤 목적으로 만들어졌는지 파악이 어려움
> - 크롤링 관련 코드와 웹/앱 백엔드 코드가 섞여 있음
> - domain/migration 폴더에 크롤링과 배치 처리 코드가 혼합됨

> [!success] 해결
> - **domain 계층 리팩토링**
>   - `domain/crawling/baemin/` - 배민 크롤링 관련 코드 통합 (auth, center, delivery, migration, partner, rider)
>   - `domain/batch/` - 배치 처리 코드 분리 (backup, headbalamt, withdrawal)
>   - `domain/portal/` - 포털 비즈니스 로직 (balance, supplier 등 추가)
>   - `domain/mobile/` - 모바일 앱 비즈니스 로직
>
> - **infrastructure 계층 리팩토링**
>   - `infrastructure/crawling/` - 크롤링 인프라 (webclient, crypto, http)
>   - `infrastructure/payment/hecto/` - Hecto 결제 관련 분리
>   - `infrastructure/crypto/` - 일반 암호화 유틸만 유지
>
> - **app 계층 리팩토링**
>   - `app/controller/crawling/` - 크롤링 API (auth, center, delivery, migration, partner, rider)
>   - `app/controller/batch/` - 배치 처리 API
>   - `app/controller/portal/` - 포털 API (balance, supplier 이동)
>   - `app/request/crawling/` - 크롤링 요청 DTO
>   - `app/response/crawling/` - 크롤링 응답 DTO
>   - `app/response/batch/` - 배치 응답 DTO

> [!tip] 배운 점
> - Clean Architecture에서 계층 간 의존성 방향이 중요함
> - 크롤링 모델을 사용하는 Converter는 크롤링 도메인에 위치해야 함
> - Controller는 절대 domain 계층에 있으면 안 됨 (app 계층에 위치)
> - JPA Repository는 Entity 기준으로 분류해도 무방하나, 도메인 인터페이스 구현체는 도메인 구조를 따라야 함

---

## 2. 최종 폴더 구조

```
api/src/main/java/com/vroong/api/
├── domain/
│   ├── crawling/baemin/      # 배민 크롤링 (79개+ 파일)
│   │   ├── auth/
│   │   ├── center/
│   │   ├── delivery/
│   │   ├── migration/
│   │   ├── partner/
│   │   └── rider/
│   ├── batch/                # 배치 처리 (10개 파일)
│   │   ├── backup/
│   │   ├── headbalamt/
│   │   └── withdrawal/
│   ├── portal/               # 포털 비즈니스 (90개+ 파일)
│   ├── mobile/               # 모바일 앱 비즈니스
│   └── common/               # 공통 코드
│
├── infrastructure/
│   ├── crawling/             # 크롤링 인프라 (14개 파일)
│   │   ├── crypto/
│   │   ├── http/
│   │   └── webclient/
│   ├── payment/hecto/        # Hecto 결제 (3개 파일)
│   ├── crypto/               # 일반 암호화
│   └── persistence/          # DB 관련
│
└── app/
    ├── controller/
    │   ├── crawling/         # 크롤링 API
    │   ├── batch/            # 배치 API
    │   ├── portal/           # 포털 API
    │   └── mobile/           # 모바일 API
    ├── request/
    │   ├── crawling/         # 크롤링 요청
    │   ├── portal/           # 포털 요청
    │   └── mobile/           # 모바일 요청
    └── response/
        ├── crawling/         # 크롤링 응답
        ├── batch/            # 배치 응답
        ├── portal/           # 포털 응답
        └── mobile/           # 모바일 응답
```

---

## 3. 남은 마이너 수정 사항

| 파일 | 현재 위치 | 이동 위치 |
|------|----------|----------|
| TableBackupResponse.java | response/crawling/migration/ | response/batch/ |
| MobAdminSwitchController.java | domain/mobile/controller/ | app/controller/mobile/ |
| RiderInfoEntityConverter.java 등 4개 | domain/portal/rider/converter/ | domain/crawling/baemin/rider/converter/ |
| CenterRepositoryImpl.java | repository/center/ | repository/crawling/center/ |

---

## 📋 TODO

- [x] domain 폴더 리팩토링
- [x] infrastructure 폴더 리팩토링
- [x] app/controller 폴더 리팩토링
- [x] app/request 폴더 리팩토링
- [x] app/response 폴더 리팩토링
- [x] 남은 마이너 수정 사항 처리 (6개 파일) ✅ 2026-02-24
- [x] 컴파일 테스트 및 import 경로 확인 ✅ 2026-02-24

---

## 📝 메모

- 크롤링 = 배민 API에서 데이터를 가져오는 모든 코드
- 배치 = 스케줄러로 실행되는 대량 처리 (헤드 잔액 계산, 출금제한, 테이블 백업)
- 포털 = 웹 포털에서 사용하는 비즈니스 로직
- 모바일 = 라이더 앱에서 사용하는 비즈니스 로직
