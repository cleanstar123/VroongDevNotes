# 트러블슈팅 3월 2주차 (2026-03-09 ~ 2026-03-14)

#troubleshooting #vroong #race-condition #database #pessimistic-lock #balance

---

## 📊 이슈 요약

| 이슈 | 발생일 | 상태 | 심각도 |
|------|:------:|:----:|:------:|
| 라이더 잔액 50,000원 누락 (Race Condition) | 2026-03-13 | 🔍 분석 완료 | 🔴 Critical |
| 정산 중 출금 시 Race Condition 위험 | 2026-03-13 | 🔍 분석 완료 | 🟠 High |

---

## 1.  라이더 잔액 50,000원 누락 - Race Condition 문제

### 개요

| 항목 | 내용 |
|------|------|
| **발생 시각** | 2026-03-13 14:10 ~ 14:11 |
| **영향 범위** | 라이더 잔액 정산 |
| **심각도** | 🔴 Critical |
| **관련 시스템** | vroong-api, VR_ACCOUNT_MST 테이블 |

### 증상

- 라이더 잔액이 예상보다 50,000원 적게 기록됨
- SEQ 65827 (TI - 입금)과 SEQ 66237 (RP - 선정산) 레코드에서 BAL_AMT 불일치
- 두 트랜잭션이 거의 동시에 발생하여 잔액 계산 오류

### 문제 데이터

| SEQ_NO | ACCT_TYPE | ACCT_AMT | BAL_AMT     | INS_DATE            |
| ------ | --------- | -------- | ----------- | ------------------- |
| 65827  | TI (입금)   | 50,000   | 50,320      | 2026-03-13 14:10:36 |
| 66237  | RP (선정산)  | 99,912   | **100,232** | 2026-03-13 14:11:04 |

**예상 BAL_AMT**: 50,320 + 99,912 = **150,232**
**실제 BAL_AMT**: **100,232** (50,000원 누락)

### 장애 타임라인

| 시간       | 이벤트          | 상세                                          |
| -------- | ------------ | ------------------------------------------- |
| 14:10:36 | TI 레코드 생성 시작 | HectoDepositUseCase에서 입금 처리 시작              |
| 14:10:36 | TI 트랜잭션 A    | 잔액 조회 (320원) → 계산 (50,320) → 저장             |
| 14:11:04 | RP 레코드 생성 시작 | BaeminToVroongIfUseCase에서 선정산 처리 시작         |
| 14:11:04 | RP 트랜잭션 B    | 잔액 조회 (**320원 - 락 없음**) → 계산 (100,232) → 저장 |
| 14:11:04 | 문제 발생        | TI 커밋 전에 RP가 stale 데이터 읽음                   |

### 원인 분석

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Race Condition 발생 흐름                                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Timeline (밀리초 단위로 거의 동시 발생)                                  │
│  ─────────────────────────────────────                                  │
│                                                                         │
│  T1 (TI 입금 처리)                    T2 (RP 선정산 처리)                │
│  ────────────────                    ─────────────────                  │
│       │                                    │                            │
│       ▼                                    │                            │
│  [14:10:36.xxx]                            │                            │
│  BEGIN TRANSACTION                         │                            │
│       │                                    │                            │
│       ▼                                    │                            │
│  잔액 조회 (락 없음)                        │                            │
│  → BAL_AMT = 320원                         │                            │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  계산: 320 + 50,000                 [14:11:04.xxx]                      │
│       │                              BEGIN TRANSACTION                  │
│       │                                    │                            │
│       │                                    ▼                            │
│       │                              잔액 조회 (락 없음) ← 문제 발생!    │
│       │                              → BAL_AMT = 320원 (stale)          │
│       │                                    │                            │
│       ▼                                    │                            │
│  INSERT (BAL_AMT=50,320)                   │                            │
│       │                                    ▼                            │
│       ▼                              계산: 320 + 99,912                 │
│  COMMIT                                    │                            │
│                                            ▼                            │
│                                      INSERT (BAL_AMT=100,232)            │
│                                            │                            │
│                                            ▼                            │
│                                      COMMIT                             │
│                                                                         │
│  결과: TI의 50,000원이 RP 잔액 계산에 반영되지 않음                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 문제 코드 분석

#### 1. 잔액 조회 메서드 - 락 없음 (문제의 핵심)

**파일**: `VrAccountMstQueryDslRepository.java:33-47`

```java
/**
 * 특정 라이더의 최신 잔액 조회 (락 없음 - Race Condition 발생 원인)
 */
public BigDecimal findLatestBalAmtBySupplyIdAndRiderId(String supplyId, String riderId) {
  return queryFactory
      .select(accountMst.balAmt)
      .from(accountMst)
      .where(
          accountMst.riderId.eq(riderId)  // ⚠️ supplyId가 사용되지 않음!
              .and(accountMst.acctType.notIn("RD", "SD")))
      .orderBy(accountMst.seqNo.desc())
      .limit(1)
      .fetchOne();
}
```

**문제점**:
1. **Pessimistic Lock 미적용**: 동시 트랜잭션이 같은 데이터를 읽을 수 있음
2. **supplyId 파라미터 미사용**: 파라미터로 받지만 쿼리에서 사용하지 않음

#### 2. 락이 있는 메서드 (해결책)

**파일**: `VrAccountMstQueryDslRepository.java:50-66`

```java
/**
 * 특정 라이더의 최신 잔액 조회 (락 적용 - Race Condition 방지)
 */
public BigDecimal findLatestBalAmtBySupplyIdAndRiderIdWithLock(String supplyId, String riderId) {
  return queryFactory
      .select(accountMst.balAmt)
      .from(accountMst)
      .where(
          accountMst.riderId.eq(riderId)
              .and(accountMst.acctType.notIn("RD", "SD")))
      .orderBy(accountMst.seqNo.desc())
      .limit(1)
      .setLockMode(jakarta.persistence.LockModeType.PESSIMISTIC_WRITE)  // ✅ 락 적용
      .fetchOne();
}
```

#### 3. RP 레코드 생성 시 잔액 조회 (락 없는 메서드 사용)

**파일**: `BaeminToVroongIfUseCase.java:1420-1422`

```java
// ⚠️ 락 없는 메서드 사용 - Race Condition 발생 가능
BigDecimal latestBalAmt =
    vrAccountMstQueryDslRepository.findLatestBalAmtBySupplyIdAndRiderId(
        savedAcctDaily.getSupplyId(), savedAcctDaily.getRiderId());
```

#### 4. 잔액 계산 로직

**파일**: `BaeminToVroongIfEntityConverter.java:386-392`

```java
// BAL_AMT = 최신 잔액 + 정산 금액
BigDecimal balAmt =
    (latestBalAmt != null ? latestBalAmt : BigDecimal.ZERO)
        .add(riderAcctDaily.getTotAcctVal() != null
            ? riderAcctDaily.getTotAcctVal() : BigDecimal.ZERO);
```

### 영향받는 코드 위치

| 파일 | 라인 | 설명 |
|------|------|------|
| `VrAccountMstQueryDslRepository.java` | 33-47 | 락 없는 잔액 조회 메서드 |
| `BaeminToVroongIfUseCase.java` | 1420-1422 | RP 레코드 생성 시 잔액 조회 |
| `HectoDepositUseCase.java` | 130-131 | TI 레코드 생성 시 잔액 조회 (추정) |
| `BaeminToVroongIfEntityConverter.java` | 386-392 | 잔액 계산 로직 |

### 해결 방법

#### 방법 1: 락이 있는 메서드로 교체

```java
// 변경 전 (BaeminToVroongIfUseCase.java:1420-1422)
BigDecimal latestBalAmt =
    vrAccountMstQueryDslRepository.findLatestBalAmtBySupplyIdAndRiderId(
        savedAcctDaily.getSupplyId(), savedAcctDaily.getRiderId());

// 변경 후
BigDecimal latestBalAmt =
    vrAccountMstQueryDslRepository.findLatestBalAmtBySupplyIdAndRiderIdWithLock(
        savedAcctDaily.getSupplyId(), savedAcctDaily.getRiderId());
```

#### 방법 2: 모든 잔액 조회 위치에 락 적용

잔액을 조회하는 모든 위치에서 `findLatestBalAmtBySupplyIdAndRiderIdWithLock` 메서드 사용:

1. `BaeminToVroongIfUseCase.java` - RP 레코드 생성
2. `HectoDepositUseCase.java` - TI 레코드 생성
3. 기타 잔액 변경이 발생하는 모든 UseCase

### Pessimistic Lock 동작 원리

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Pessimistic Lock 적용 후 흐름                                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  T1 (TI 입금 처리)                    T2 (RP 선정산 처리)                │
│  ────────────────                    ─────────────────                  │
│       │                                    │                            │
│       ▼                                    │                            │
│  BEGIN TRANSACTION                         │                            │
│       │                                    │                            │
│       ▼                                    │                            │
│  잔액 조회 (PESSIMISTIC_WRITE)            │                             │
│  → 락 획득 🔒                              │                            │
│  → BAL_AMT = 320원                         │                            │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  계산: 320 + 50,000                  BEGIN TRANSACTION                  │
│       │                                    │                            │
│       │                                    ▼                            │
│       │                              잔액 조회 (PESSIMISTIC_WRITE)       │
│       │                              → 락 대기 ⏳ (T1 커밋 대기)         │
│       │                                    │                            │
│       ▼                                    │ (대기 중...)               │
│  INSERT (BAL_AMT=50,320)                   │                            │
│       │                                    │                            │
│       ▼                                    │                            │
│  COMMIT                                    │                            │
│  → 락 해제 🔓                              │                            │
│                                            ▼                            │
│                                      락 획득 🔒                         │
│                                      → BAL_AMT = 50,320 ✅             │
│                                            │                            │
│                                            ▼                            │
│                                      계산: 50,320 + 99,912              │
│                                            │                            │
│                                            ▼                            │
│                                      INSERT (BAL_AMT=150,232) ✅        │
│                                            │                            │
│                                            ▼                            │
│                                      COMMIT                             │
│                                                                         │
│  결과: 정상적으로 잔액 누적                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 검증 방법

1. **단위 테스트**: 동시 트랜잭션 시뮬레이션 테스트 작성
2. **통합 테스트**: 실제 환경에서 동시 입금/정산 테스트
3. **모니터링**: 잔액 불일치 감지 쿼리 추가

```sql
-- 잔액 불일치 감지 쿼리
SELECT
    a.SEQ_NO,
    a.RIDER_ID,
    a.ACCT_TYPE,
    a.ACCT_AMT,
    a.BAL_AMT,
    a.INS_DATE,
    LAG(a.BAL_AMT) OVER (PARTITION BY a.RIDER_ID ORDER BY a.SEQ_NO) as PREV_BAL_AMT,
    a.BAL_AMT - (LAG(a.BAL_AMT) OVER (PARTITION BY a.RIDER_ID ORDER BY a.SEQ_NO) + a.ACCT_AMT) as DIFF
FROM VR_ACCOUNT_MST a
WHERE a.ACCT_TYPE NOT IN ('RD', 'SD')
HAVING DIFF != 0;
```

---

## 2. 정산 중 출금 시 Race Condition 위험 - 추가 분석

### 개요

| 항목 | 내용 |
|------|------|
| **분석 일자** | 2026-03-13 |
| **영향 범위** | 선정산, 주정산, 출금, 송금 전체 |
| **심각도** | 🟠 High |
| **상태** | 잠재적 위험 (추가 검토 필요) |

### 문제 정의

1번 이슈(TI-RP Race Condition)를 분석하면서 발견된 추가 위험 시나리오:
- **정산 계산 중에 라이더가 출금하면?**
- **주정산 업로드 중에 입금이 발생하면?**

### 잔액 조회 시 락 사용 현황

| 기능 | 파일:라인 | 메서드 | 락 사용 |
|------|-----------|--------|:-------:|
| **모바일 출금** | `MobWithdrlUseCase.java:206` | `getWithdrlAccountInfoWithLock` | ✅ |
| 모바일 입금 | `MobWithdrlUseCase.java:503` | `getWithdrlAccountInfo` | ❌ |
| 모바일 송금 | `MobWithdrlUseCase.java:704` | `getWithdrlAccountInfo` | ❌ |
| **라이더 주정산 (RW)** | `RiderSettlementUseCase.java:1389` | `findLatestBalAmtBySupplyIdAndRiderId` | ❌ |
| 라이더 재등록 (RR) | `RiderSettlementUseCase.java:1368` | `findLatestBalAmtByRiderIdExcludeTypes` | ❌ |
| **협력사 주정산 (SW)** | `PartnerSettlementUseCase.java:2031` | `findLatestBalAmtBySupplyId` | ❌ |
| 협력사 재등록 (SR) | `PartnerSettlementUseCase.java:2010` | `findLatestBalAmtBySupplyIdExcludeTypes` | ❌ |
| 선정산 (RP) | `BaeminToVroongIfUseCase.java:1421` | `findLatestBalAmtBySupplyIdAndRiderId` | ❌ |
| 입금 (TI) | `HectoDepositUseCase.java:131` | `findLatestBalAmtBySupplyIdAndRiderId` | ❌ |

### 위험 시나리오 1: 주정산 중 출금 발생

```
┌─────────────────────────────────────────────────────────────────────────┐
│  시나리오: 라이더 잔액 100,000원, 주정산 +30,000원, 출금 -50,000원       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  T1 (주정산 RW)                       T2 (출금 W)                        │
│  ─────────────                       ──────────                         │
│       │                                    │                            │
│       ▼                                    │                            │
│  잔액 조회 (락 없음)                        │                            │
│  → 100,000원                               │                            │
│       │                                    │                            │
│       │                                    ▼                            │
│       │                              잔액 조회 (락 있음)                 │
│       │                              → 100,000원                        │
│       │                                    │                            │
│       │                                    ▼                            │
│       │                              계산: 100,000 - 50,000             │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  계산: 100,000 + 30,000              INSERT (BAL_AMT=50,000)            │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  INSERT (BAL_AMT=130,000)            COMMIT                             │
│       │                                                                 │
│       ▼                                                                 │
│  COMMIT                                                                 │
│                                                                         │
│  ⚠️ 예상 최종 잔액: 100,000 + 30,000 - 50,000 = 80,000원                │
│  ❌ 실제 최종 잔액: 130,000원 (출금 50,000원이 반영 안 됨!)              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 위험 시나리오 2: 선정산(RP) 중 입금(TI) 발생 (1번 이슈)

1번 이슈에서 이미 발생한 실제 케이스.

### 위험 시나리오 3: 송금 중 정산 발생

```
┌─────────────────────────────────────────────────────────────────────────┐
│  시나리오: A 라이더 → B 협력사 송금 중 B 협력사 주정산 발생               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  T1 (송금 - 락 없음)                  T2 (협력사 주정산 - 락 없음)        │
│  ──────────────────                  ─────────────────────────          │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  B 협력사 잔액 조회                   B 협력사 잔액 조회                  │
│  → 200,000원                         → 200,000원                        │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  계산: 200,000 + 50,000              계산: 200,000 + 100,000            │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  INSERT (BAL_AMT=250,000)            INSERT (BAL_AMT=300,000)           │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  COMMIT                              COMMIT                             │
│                                                                         │
│  ⚠️ 예상 최종 잔액: 200,000 + 50,000 + 100,000 = 350,000원              │
│  ❌ 실제 최종 잔액: 300,000원 (송금 50,000원이 반영 안 됨!)              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 출금이 락을 사용해도 문제가 발생하는 이유

```
┌─────────────────────────────────────────────────────────────────────────┐
│  핵심 문제: 락이 일관되게 적용되지 않음                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [상황] 출금만 락 사용, 정산은 락 미사용                                 │
│                                                                         │
│  정산 로직 ─────┐                                                       │
│       │        │ 락 없이 잔액 읽음 (stale 가능)                         │
│       ▼        │                                                        │
│  SELECT 잔액 ──┘                                                        │
│       │                                                                 │
│       │                              출금 로직 ─────┐                   │
│       │                                    │       │ 락 획득 (무의미)   │
│       │                                    ▼       │ 정산이 이미 stale  │
│       │                              SELECT 잔액 ──┘ 데이터 보유        │
│       │                                    │                            │
│       ▼                                    ▼                            │
│  INSERT (stale 기반 계산)            INSERT                             │
│                                                                         │
│  결론: 모든 잔액 변경 로직에서 일관되게 락을 사용해야 함                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 수정 필요 위치 (전체 목록)

| 파일 | 라인 | 현재 메서드 | 수정 방향 |
|------|------|------------|----------|
| `RiderSettlementUseCase.java` | 1389 | `findLatestBalAmtBySupplyIdAndRiderId` | `...WithLock` 으로 교체 |
| `RiderSettlementUseCase.java` | 1368 | `findLatestBalAmtByRiderIdExcludeTypes` | WithLock 버전 신규 생성 |
| `PartnerSettlementUseCase.java` | 2031 | `findLatestBalAmtBySupplyId` | `findLatestBalAmtBySupplyIdWithLock` 으로 교체 |
| `PartnerSettlementUseCase.java` | 2010 | `findLatestBalAmtBySupplyIdExcludeTypes` | WithLock 버전 신규 생성 |
| `BaeminToVroongIfUseCase.java` | 1421, 1437 | `findLatestBalAmtBySupplyIdAndRiderId` | `...WithLock` 으로 교체 |
| `HectoDepositUseCase.java` | 131, 264 | `findLatestBalAmtBySupplyIdAndRiderId` | `...WithLock` 으로 교체 |
| `MobWithdrlUseCase.java` | 503 | `getWithdrlAccountInfo` | `...WithLock` 으로 교체 (입금) |
| `MobWithdrlUseCase.java` | 704 | `getWithdrlAccountInfo` | `...WithLock` 으로 교체 (송금) |

### 신규 WithLock 메서드 생성 필요

`VrAccountMstQueryDslRepository.java`에 다음 메서드 추가 필요:

```java
// 1. 라이더 잔액 조회 (특정 타입 제외) - WithLock
public BigDecimal findLatestBalAmtByRiderIdExcludeTypesWithLock(
    String riderId, List<String> excludeTypes) {
  return queryFactory
      .select(accountMst.balAmt)
      .from(accountMst)
      .where(
          accountMst.riderId.eq(riderId)
              .and(accountMst.acctType.notIn(excludeTypes)))
      .orderBy(accountMst.seqNo.desc())
      .limit(1)
      .setLockMode(LockModeType.PESSIMISTIC_WRITE)
      .fetchOne();
}

// 2. 협력사 잔액 조회 (특정 타입 제외) - WithLock
public BigDecimal findLatestBalAmtBySupplyIdExcludeTypesWithLock(
    String supplyId, List<String> excludeTypes) {
  return queryFactory
      .select(accountMst.balAmt)
      .from(accountMst)
      .where(
          accountMst.supplyId.eq(supplyId)
              .and(accountMst.acctType.notIn(excludeTypes)))
      .orderBy(accountMst.seqNo.desc())
      .limit(1)
      .setLockMode(LockModeType.PESSIMISTIC_WRITE)
      .fetchOne();
}
```

### 후속 조치 (2번 이슈 관련)

- [ ] `RiderSettlementUseCase.java`에서 락 있는 메서드로 교체
- [ ] `PartnerSettlementUseCase.java`에서 락 있는 메서드로 교체
- [ ] `MobWithdrlUseCase.java` 입금/송금에서 락 있는 메서드로 교체
- [ ] `findLatestBalAmtByRiderIdExcludeTypesWithLock` 메서드 신규 생성
- [ ] `findLatestBalAmtBySupplyIdExcludeTypesWithLock` 메서드 신규 생성
- [ ] 동시성 테스트 케이스 작성 (정산 + 출금 동시 실행)

---

## 📝 교훈 및 개선 사항

### 근본 원인

- **동시성 제어 미흡**: 잔액 조회 시 Pessimistic Lock이 적용되지 않아 Race Condition 발생
- **코드 중복**: 락이 있는 메서드와 없는 메서드가 별도로 존재하여 혼란 유발

### 주요 교훈

1. **금융 데이터는 반드시 락 사용**
   - 잔액, 거래 내역 등 금융 관련 데이터는 Pessimistic Lock 필수
   - 동시성 문제는 재현이 어려워 사전 예방이 중요

2. **메서드 네이밍 컨벤션**
   - 락이 있는 메서드와 없는 메서드 구분 명확히
   - 또는 기본 메서드에 락을 적용하고 특수 케이스만 락 없이 사용

3. **파라미터 검증**
   - 사용하지 않는 파라미터 제거 또는 사용하도록 수정
   - `supplyId` 파라미터가 쿼리에서 사용되지 않는 문제 발견

### 후속 조치 (통합)

**1번 이슈 - 선정산/입금 Race Condition:**
- [ ] `BaeminToVroongIfUseCase.java`에서 락 있는 메서드로 교체
- [ ] `HectoDepositUseCase.java`에서 락 있는 메서드로 교체
- [ ] 락 없는 메서드 `findLatestBalAmtBySupplyIdAndRiderId` 제거 또는 Deprecated 처리
- [ ] 잔액 불일치 모니터링 쿼리 추가
- [ ] 기존 잔액 데이터 정합성 점검 및 수정

**2번 이슈 - 정산/출금/송금 Race Condition:**
- [ ] `RiderSettlementUseCase.java`에서 락 있는 메서드로 교체
- [ ] `PartnerSettlementUseCase.java`에서 락 있는 메서드로 교체
- [ ] `MobWithdrlUseCase.java` 입금/송금에서 락 있는 메서드로 교체
- [ ] `findLatestBalAmtByRiderIdExcludeTypesWithLock` 메서드 신규 생성
- [ ] `findLatestBalAmtBySupplyIdExcludeTypesWithLock` 메서드 신규 생성
- [ ] 동시성 테스트 케이스 작성 (정산 + 출금 동시 실행)

---

## 📋 참고: JPA Pessimistic Lock 타입

| LockModeType | 설명 | 사용 케이스 |
|--------------|------|------------|
| PESSIMISTIC_READ | 공유 락 (읽기 허용, 쓰기 차단) | 읽기 전용 트랜잭션 |
| PESSIMISTIC_WRITE | 배타 락 (읽기/쓰기 모두 차단) | 잔액 변경 등 쓰기 작업 |
| PESSIMISTIC_FORCE_INCREMENT | 배타 락 + 버전 증가 | 버전 관리 필요 시 |

### QueryDSL에서 Pessimistic Lock 적용

```java
// QueryDSL에서 락 적용 방법
queryFactory
    .select(accountMst.balAmt)
    .from(accountMst)
    .where(...)
    .setLockMode(LockModeType.PESSIMISTIC_WRITE)  // 락 적용
    .fetchOne();
```

### JPA Repository에서 Pessimistic Lock 적용

```java
// @Lock 어노테이션 사용
@Lock(LockModeType.PESSIMISTIC_WRITE)
@Query("SELECT a.balAmt FROM VrAccountMst a WHERE a.riderId = :riderId ORDER BY a.seqNo DESC LIMIT 1")
BigDecimal findLatestBalAmtWithLock(@Param("riderId") String riderId);
```
